# Home Assistant Configuration
HA_URL=https://homeassistant.local:8123
HA_TOKEN=your-long-lived-access-token-here

# Transport mode (stdio for Desktop Extension, http for Docker)
TRANSPORT=stdio

# HTTP server port (only used when TRANSPORT=http)
PORT=3000

# Request timeout in seconds
REQUEST_TIMEOUT=30
